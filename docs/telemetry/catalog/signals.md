
<!-- Code generated by OpenTelemetry Weaver. DO NOT EDIT. -->

# Telemetry signals

This slice catalogs the standard HTTP metrics emitted when metrics export is
enabled and the application-specific HTTP client and server span conventions.
Attributes marked `Opt In` belong to the upstream HTTP convention but are not
emitted unless the instrumentation is explicitly configured to capture them.


## `http.client.request.body.size`

- Convention: `metric.http.client.request.body.size`
- Signal type: metric, histogram instrument
- Stability: development

Size of HTTP client request bodies.


The size of the request payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `server.address` | string | Required | stable | Server domain name if available without reverse DNS lookup; otherwise, IP address or Unix domain socket name. |
| `server.port` | int | Required | stable | Server port number. |
| `url.scheme` | string | Opt In | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `url.template` | string | Conditional: If available. | development | The low-cardinality template of an [absolute path reference](https://www.rfc-editor.org/rfc/rfc3986#section-4.2). |

## `http.client.request.duration`

- Convention: `metric.http.client.request.duration`
- Signal type: metric, histogram instrument
- Stability: stable

Duration of HTTP client requests.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `server.address` | string | Required | stable | Server domain name if available without reverse DNS lookup; otherwise, IP address or Unix domain socket name. |
| `server.port` | int | Required | stable | Server port number. |
| `url.scheme` | string | Opt In | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `url.template` | string | Opt In | development | The low-cardinality template of an [absolute path reference](https://www.rfc-editor.org/rfc/rfc3986#section-4.2). |

## `http.server.request.body.size`

- Convention: `metric.http.server.request.body.size`
- Signal type: metric, histogram instrument
- Stability: development

Size of HTTP server request bodies.


The size of the request payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `http.route` | string | Conditional: If and only if it's available | stable | The matched route template for the request. This MUST be low-cardinality and include all static path segments, with dynamic path segments represented with placeholders. |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `server.address` | string | Opt In | stable | Name of the local HTTP server that received the request. |
| `server.port` | int | Opt In | stable | Port of the local HTTP server that received the request. |
| `url.scheme` | string | Required | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `user_agent.synthetic.type` | enum | Opt In | development | Specifies the category of synthetic traffic, such as tests or bots. |

## `http.server.request.duration`

- Convention: `metric.http.server.request.duration`
- Signal type: metric, histogram instrument
- Stability: stable

Duration of HTTP server requests.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `http.route` | string | Conditional: If and only if it's available | stable | The matched route template for the request. This MUST be low-cardinality and include all static path segments, with dynamic path segments represented with placeholders. |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `server.address` | string | Opt In | stable | Name of the local HTTP server that received the request. |
| `server.port` | int | Opt In | stable | Port of the local HTTP server that received the request. |
| `url.scheme` | string | Required | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `user_agent.synthetic.type` | enum | Opt In | development | Specifies the category of synthetic traffic, such as tests or bots. |

## `http.server.response.body.size`

- Convention: `metric.http.server.response.body.size`
- Signal type: metric, histogram instrument
- Stability: development

Size of HTTP server response bodies.


The size of the response payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `http.route` | string | Conditional: If and only if it's available | stable | The matched route template for the request. This MUST be low-cardinality and include all static path segments, with dynamic path segments represented with placeholders. |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `server.address` | string | Opt In | stable | Name of the local HTTP server that received the request. |
| `server.port` | int | Opt In | stable | Port of the local HTTP server that received the request. |
| `url.scheme` | string | Required | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `user_agent.synthetic.type` | enum | Opt In | development | Specifies the category of synthetic traffic, such as tests or bots. |

## `span.nwsl.http.client`

- Convention: `span.nwsl.http.client`
- Signal type: span, client kind
- Stability: development

An outbound HTTP request from the NWSL Season ASA client.


The ASA client creates one CLIENT span for each HTTP attempt, including retries.
Its current span name is `HTTP {method} {url.path}`. The path is the escaped ASA
request path and can contain a season, stage, or requested game identifier.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.body.size` | int | Opt In | development | The size of the request payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size. |
| `http.request.header` | string[] template | Opt In | stable | HTTP request headers, `<key>` being the normalized HTTP Header name (lowercase), the value being the header values. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.request.method_original` | string | Conditional: If and only if it's different than `http.request.method`. | stable | Original HTTP method sent by the client in the request line. |
| `http.request.resend_count` | int | Recommended: if and only if request was retried. | stable | The ordinal number of request resending attempt (for any reason, including redirects). |
| `http.request.size` | int | Opt In | development | The total size of the request in bytes. This should be the total number of bytes sent over the wire, including the request line (HTTP/1.1), framing (HTTP/2 and HTTP/3), headers, and request body if any. |
| `http.response.body.size` | int | Opt In | development | The size of the response payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size. |
| `http.response.header` | string[] template | Opt In | stable | HTTP response headers, `<key>` being the normalized HTTP Header name (lowercase), the value being the header values. |
| `http.response.size` | int | Opt In | development | The total size of the response in bytes. This should be the total number of bytes sent over the wire, including the status line (HTTP/1.1), framing (HTTP/2 and HTTP/3), headers, and response body and trailers if any. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `network.peer.address` | string | Recommended | stable | Peer address of the network connection - IP address or Unix domain socket name. |
| `network.peer.port` | int | Recommended: If `network.peer.address` is set. | stable | Peer port number of the network connection. |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `network.transport` | enum | Opt In | stable | [OSI transport layer](https://wikipedia.org/wiki/Transport_layer) or [inter-process communication method](https://wikipedia.org/wiki/Inter-process_communication). |
| `server.address` | string | Required | stable | Server domain name if available without reverse DNS lookup; otherwise, IP address or Unix domain socket name. |
| `server.port` | int | Required | stable | Server port number. |
| `url.full` | string | Required | stable | Absolute URL describing a network resource according to [RFC3986](https://www.rfc-editor.org/rfc/rfc3986) |
| `url.scheme` | string | Opt In | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `url.template` | string | Opt In | development | The low-cardinality template of an [absolute path reference](https://www.rfc-editor.org/rfc/rfc3986#section-4.2). |
| `user_agent.original` | string | Opt In | stable | Value of the [HTTP User-Agent](https://www.rfc-editor.org/rfc/rfc9110.html#field.user-agent) header sent by the client. |
| `user_agent.synthetic.type` | enum | Opt In | development | Specifies the category of synthetic traffic, such as tests or bots. |

## `span.nwsl.http.server`

- Convention: `span.nwsl.http.server`
- Signal type: span, server kind
- Stability: development

An inbound request handled by the NWSL Season HTTP server.


The server uses the matched Go `http.ServeMux` pattern as the span name, for
example `GET /seasons/{season}/{stage}`. Unmatched requests use
`{method} unknown_route`.


| Attribute | Type | Requirement | Stability | Description |
| --- | --- | --- | --- | --- |
| `client.address` | string | Recommended | stable | Client address - domain name if available without reverse DNS lookup; otherwise, IP address or Unix domain socket name. |
| `client.port` | int | Opt In | stable | The port of whichever client was captured in `client.address`. |
| `error.type` | enum | Conditional: If request has ended with an error. | stable | Describes a class of error the operation ended with. |
| `http.request.body.size` | int | Opt In | development | The size of the request payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size. |
| `http.request.header` | string[] template | Opt In | stable | HTTP request headers, `<key>` being the normalized HTTP Header name (lowercase), the value being the header values. |
| `http.request.method` | enum | Required | stable | HTTP request method. |
| `http.request.method_original` | string | Conditional: If and only if it's different than `http.request.method`. | stable | Original HTTP method sent by the client in the request line. |
| `http.request.size` | int | Opt In | development | The total size of the request in bytes. This should be the total number of bytes sent over the wire, including the request line (HTTP/1.1), framing (HTTP/2 and HTTP/3), headers, and request body if any. |
| `http.response.body.size` | int | Opt In | development | The size of the response payload body in bytes. This is the number of bytes transferred excluding headers and is often, but not always, present as the [Content-Length](https://www.rfc-editor.org/rfc/rfc9110.html#field.content-length) header. For requests using transport encoding, this should be the compressed size. |
| `http.response.header` | string[] template | Opt In | stable | HTTP response headers, `<key>` being the normalized HTTP Header name (lowercase), the value being the header values. |
| `http.response.size` | int | Opt In | development | The total size of the response in bytes. This should be the total number of bytes sent over the wire, including the status line (HTTP/1.1), framing (HTTP/2 and HTTP/3), headers, and response body and trailers if any. |
| `http.response.status_code` | int | Conditional: If and only if one was received/sent. | stable | [HTTP response status code](https://tools.ietf.org/html/rfc7231#section-6). |
| `http.route` | string | Conditional: If and only if it's available | stable | The matched route template for the request. This MUST be low-cardinality and include all static path segments, with dynamic path segments represented with placeholders. |
| `network.local.address` | string | Opt In | stable | Local socket address. Useful in case of a multi-IP host. |
| `network.local.port` | int | Opt In | stable | Local socket port. Useful in case of a multi-port host. |
| `network.peer.address` | string | Recommended | stable | Peer address of the network connection - IP address or Unix domain socket name. |
| `network.peer.port` | int | Recommended: If `network.peer.address` is set. | stable | Peer port number of the network connection. |
| `network.protocol.name` | string | Conditional: If not `http` and `network.protocol.version` is set. | stable | [OSI application layer](https://wikipedia.org/wiki/Application_layer) or non-OSI equivalent. |
| `network.protocol.version` | string | Recommended | stable | The actual version of the protocol used for network communication. |
| `network.transport` | enum | Opt In | stable | [OSI transport layer](https://wikipedia.org/wiki/Transport_layer) or [inter-process communication method](https://wikipedia.org/wiki/Inter-process_communication). |
| `server.address` | string | Recommended | stable | Name of the local HTTP server that received the request. |
| `server.port` | int | Conditional: If available and `server.address` is set. | stable | Port of the local HTTP server that received the request. |
| `url.path` | string | Required | stable | The [URI path](https://www.rfc-editor.org/rfc/rfc3986#section-3.3) component |
| `url.query` | string | Conditional: If and only if one was received/sent. | stable | The [URI query](https://www.rfc-editor.org/rfc/rfc3986#section-3.4) component |
| `url.scheme` | string | Required | stable | The [URI scheme](https://www.rfc-editor.org/rfc/rfc3986#section-3.1) component identifying the used protocol. |
| `user_agent.original` | string | Recommended | stable | Value of the [HTTP User-Agent](https://www.rfc-editor.org/rfc/rfc9110.html#field.user-agent) header sent by the client. |
| `user_agent.synthetic.type` | enum | Opt In | development | Specifies the category of synthetic traffic, such as tests or bots. |
